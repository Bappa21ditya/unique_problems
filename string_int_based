123 Number Flip

int solve(int n) {
    string s=to_string(n);
    int mx=0;
    for(int i=0;i<s.size();i++)
    {
        string g=s;
        g[i]='3';
        int x=stoi(g);
        mx=max(mx,x);
    }
    return mx;
}
